/BEGIN users/_follow.html.haml

= form_for current_user.relationships.build(:followed_id => @user.id),
      :remote => true do |f|
  %div= f.hidden_field :followed_id
  .actions= f.submit "Follow"

/END users/_follow.html.haml
