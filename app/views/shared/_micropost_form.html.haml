/BEGIN shared/_micropost_form.html.haml

= form_for @micropost do |f|
  = render 'shared/error_messages', :object => f.object
  .field
    = f.text_area :content

  .actions
    = f.submit "Submit"

/END shared/_micropost_form.html.haml
