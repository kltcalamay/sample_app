<%= render :partial => 'menu' %>

<table>
  <tr>
    <th>Sender</th>
    <th>Message</th>
  </tr>
  <% @direct_messages.each do |dm| %>
    <tr>
      <td><%= dm.sender.name %></td>
      <td><%= dm.content %></td>
    </tr>
  <% end %>
</table>